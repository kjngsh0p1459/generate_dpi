name: apps
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  freezed: 2.3.2
  freezed_annotation: 2.2.0
  file_picker: ^4.0.0
  flutter_bloc: 8.0.1
  rxdart: ^0.27.0
  resources:
    path: ../resources

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: 2.3.3

flutter:
  uses-material-design: true
